<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.meper</groupId>
    <artifactId>meper-chatbi-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>meper-chatbi-dialect-sqlserver</artifactId>
  <name>MEPER ChatBI :: Dialect SQL Server</name>
  <description>SQL Server 方言实现（内置 mssql-jdbc）</description>

  <dependencies>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-connector-spi</artifactId>
    </dependency>
    <!-- 内置 SQL Server 驱动（版本由 Spring Boot BOM 管理） -->
    <dependency>
      <groupId>com.microsoft.sqlserver</groupId>
      <artifactId>mssql-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mssqlserver</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
