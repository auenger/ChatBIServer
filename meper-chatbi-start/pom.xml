<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.meper</groupId>
    <artifactId>meper-chatbi-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>meper-chatbi-start</artifactId>
  <name>MEPER ChatBI :: Start</name>
  <description>组装、配置、健康检查</description>

  <dependencies>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-web</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-domain</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-policy</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-query-enforcement</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-connector-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-dialect-mysql</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-dialect-sqlserver</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-dialect-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-dialect-oracle</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-control-storage</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-session-wiki</artifactId>
    </dependency>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-agent</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!-- 控制库（MySQL）驱动：start 的直接运行时需求，不依赖方言模块传递提供 -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- 全链路集成测试：控制库与业务库均为 Testcontainers MySQL -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mysql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
