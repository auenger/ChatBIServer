<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.meper</groupId>
    <artifactId>meper-chatbi-server</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>meper-chatbi-connector-jdbc</artifactId>
  <name>MEPER ChatBI :: Connector JDBC</name>
  <description>驱动加载、DataSource/连接池、事务、执行与取消</description>

  <dependencies>
    <dependency>
      <groupId>com.meper</groupId>
      <artifactId>meper-chatbi-connector-spi</artifactId>
    </dependency>
    <!-- 动态业务库连接池（版本由 Spring Boot BOM 管理） -->
    <dependency>
      <groupId>com.zaxxer</groupId>
      <artifactId>HikariCP</artifactId>
    </dependency>
    <!-- 测试：Testcontainers 起真实 MySQL；驱动用 test 范围引入，不依赖任何方言模块 -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mysql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
